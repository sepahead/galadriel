[package]
name = "galadriel-pid"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The cross-sensor Partial Information Decomposition engine for Galadriel's Mirror (feature `pid`)."
publish = false

[lints]
workspace = true

[dependencies]
galadriel-core = { workspace = true }
# pid-core is pinned by public git tag in the workspace root and resolved exactly
# by Cargo.lock.
pid-core = { workspace = true }

[dev-dependencies]
galadriel-sim = { version = "0.1.0", path = "../galadriel-sim" }
