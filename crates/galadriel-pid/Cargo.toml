[package]
name = "galadriel-pid"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The cross-sensor Partial Information Decomposition engine for Galadriel's Mirror (feature `pid`)."

[dependencies]
galadriel-core = { workspace = true }
# pid-core is pinned by git tag in the workspace root (with a .cargo/config.toml `paths`
# override for local sibling-tree development).
pid-core = { workspace = true }

[dev-dependencies]
galadriel-sim = { path = "../galadriel-sim" }
