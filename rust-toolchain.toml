[toolchain]
# One exact workspace minimum supported Rust version (MSRV) keeps local,
# continuous integration (CI), and optional-feature builds reproducible.
channel = "1.89.0"
components = ["rustfmt", "clippy"]
