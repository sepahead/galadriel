---
description: Galadriel repository operating contract
alwaysApply: true
---

# Galadriel instructions

Read [README.md](README.md), then follow [AGENTS.md](AGENTS.md).
That file owns the maintainer and coding-agent contract.
[CLAUDE.md](CLAUDE.md) supplies the same forwarding entry for Markdown-based tools.
Use the linked owning documents before editing an affected surface.
Preserve advisory-only outcomes, explicit insufficiency, immutable history, and exact source gates.

## Source lifecycle markers

These markers remain explicit for the unchanged repository release validator.
The complete procedure is in [MAINTAINER_WORKFLOWS.md](docs/MAINTAINER_WORKFLOWS.md).

Source preparation state for this tree: `UNPUBLISHED_CANDIDATE` with no candidate release date.
Before the threat register enters `FROZEN_AT_CANDIDATE`, the source **SHALL** be
`DATE_BOUND_CANDIDATE` with one ISO `candidate_release_date`.
Every mode and date marker **SHALL** match that date-bound state.
`DATE_BOUND_CANDIDATE` with `LIVING_UNTIL_CANDIDATE_FREEZE` is the permitted
transition before freeze.
